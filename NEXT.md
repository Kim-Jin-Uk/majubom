# 다음에 할 일

> 세션을 끝낼 때 이 세 줄만 갱신하고 커밋에 포함한다.
> 2주 쉬었다 돌아왔을 때 이 파일이 유일한 복귀 지점이다. (`majubom-docs/08` 11.1)

## 지금 어디

**9/9 — 처음으로 실제 배포가 됐다.** 저장소 초기화(9/4)부터 App Hosting 롤아웃이 전부 실패하고 있었고,
main 은 계속 초록불이라 아무도 몰랐다. 인증·사업장·상품·근무표·예약 엔진·예약 콘솔이 그동안 라이브에 없었다.
원인 둘, 둘 다 `apphosting.yaml` 이었다 — 빈 `value: ""`(#164)와 만들지도 않은 시크릿 선언(#165).
이제 `tests/unit/apphosting-config.test.ts` 가 둘 다 CI 에서 잡는다.

에픽 #8 예약 콘솔 ★ — 목록·필터·상세·담당 변경·스코프(#60·#61·#63) 머지됨(PR #162). 남은 것은 대시보드(#59)와 캘린더 뷰·모바일(#62).
에픽 #7 예약 엔진(FR-BOOK-010~070)은 PR #158~#161 로 전부 머지됐다 — 남은 것은 알림·상담방 예약 카드(알림 에픽).
마이그레이션 0003~0007 은 **Neon 에 적용 완료**(9/9). `npm run db:status` 로 언제든 확인한다.
테스트: 슬롯 픽스처 40건 · 전이 표 검사 · 배포 설정 6건 · DB 회귀 23건(동시성 4 + 변경·워크인 7 + 콘솔 스코프 12, `DATABASE_URL` 이 test DB 일 때만).

## 막힌 것

**Cloud Scheduler 잡 셋이 아직 없다** — C1 `cleanup-unverified`(일 1회) · C2 `expire-requests`(5분) · C3 `auto-no-show`(일 1회).
인증은 헤더 `X-Cron-Secret: $CRON_SECRET` 하나뿐이고 틀리면 404 다(엔드포인트 존재를 숨긴다). 절차는 README "운영: 마이그레이션과 배치".
C2 가 안 돌면 `REQUESTED` 가 슬롯을 계속 묶는다 — 콘솔 요약의 "승인 대기" 가 늘어나는 것이 유일한 신호다.

`NEXT_PUBLIC_FIREBASE_PROJECT_ID` 가 `majubom` 인데 실제 프로젝트는 `majubom-bb43e` 로 보인다.
`admin.ts` 의 ADC 경로가 이 값을 projectId 로 쓰므로 채팅(`FEATURE_CHAT`)을 켜는 순간 엉뚱한 프로젝트로 커스텀 토큰을 발급한다. `firebase projects:list` 로 확정한 뒤 고친다.

소셜 로그인·관리자 승격은 운영 작업(코드는 준비돼 있다).
**아직 정하지 못한 것은 전부 `LATER.md` (추후 논의).** 예약 엔진에 걸린 것은 L-06(슬롯 계산 가정 A1~A11 — 명세를 고칠지) · L-31(합산 잔여의 의미) · L-32(매니저가 처리 못 하는 예약).

## 다음 한 수

1. Cloud Scheduler 잡 셋 등록 + `curl` 로 200 확인. 배치가 없으면 예약 엔진이 반쪽이다.
2. 에픽 #8 남은 조각 — 대시보드(#59: 오늘 예약·승인 대기·다음 일정) · 주간 캘린더 뷰와 모바일 레이아웃(#62).
3. 에픽 #11 예약 위젯 — `/api/public/products/:id/slots` 와 `POST /api/reservations` 를 쓰는 7단계 흐름.
4. 알림 최소본(확정·취소·거절 메일, Resend) — M2 편입분. Resend 키는 9/9 에 넣었다.
