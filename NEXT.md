# 다음에 할 일

> 세션을 끝낼 때 이 세 줄만 갱신하고 커밋에 포함한다.
> 2주 쉬었다 돌아왔을 때 이 파일이 유일한 복귀 지점이다. (`majubom-docs/08` 11.1)

## 지금 어디

**9/10 — 손님이 예약을 끝까지 넣을 수 있게 됐다.** 에픽 #10 공개 사업장 홈(#71)과 에픽 #11 예약 위젯의
[1]~[7] 이 전부 머지됐다(PR #168·#169·#170·#171). `/@{slug}` 에서 상품을 고르면 `/@{slug}/book` 으로
넘어가 날짜·시간·담당자를 고르고, 로그인 왕복을 서버 토큰(`booking_selections`, 마이그레이션 0008)으로
건너 예약이 들어간다.

에픽 #11 남은 것은 **#85(뒤로가기·키보드·WCAG AA) · #86(모바일 예약 플로우)** 둘이다.
에픽 #7 예약 엔진은 **#56(로그 적재) · #57(확정·취소·거절 메일)** 만 남았다.
에픽 #9 관리자 콘솔(#64~#70)과 에픽 #6 근무 교대(#43~#46)는 아직 손대지 않았다 — `/admin` 은 자리표시자다.
에픽 #1 잔여는 **#9(R2 리사이즈 variants) · #12(도메인·카카오)** 다. #13(슬롯 40건)은 이미 끝나 닫았다.

마이그레이션은 **0008 까지**. `npm run db:status` 로 언제든 확인한다 (0008 은 아직 Neon 미적용일 수 있다).

## 막힌 것

**손님에게 메일이 안 간다.** `MAIL_FROM` 이 `onboarding@resend.dev`(Resend 샌드박스)라 계정 소유자
본인에게만 발송된다. 위젯 완료 화면은 이미 "메일로 보내 드렸어요" 라고 말한다 — #57 코드를 만들어도
발신 도메인 검증(#12) 전에는 참이 되지 않는다.

**카카오 로그인이 없다.** `apphosting.yaml` 의 `AUTH_KAKAO_*`·`AUTH_GOOGLE_*` 이 주석이라
위젯 [6] 은 이메일 로그인만 뜬다. 기획서 R13(로그인 전환율 70%)의 전제가 카카오 원클릭이라,
이 상태에서 잰 전환율은 판정에 쓸 수 없다.

**`NEXT_PUBLIC_FIREBASE_PROJECT_ID`** 가 `majubom` 인데 실제는 `majubom-bb43e` 로 보인다.
채팅(`FEATURE_CHAT`)을 켜기 전에 `firebase projects:list` 로 확정한다.

셋 다 **운영 작업이고 9/10 에 "추후" 로 미뤄 두었다.** 코드가 막혀 있는 것은 아니다.

## 다음 한 수

1. **에픽 #11 마무리** — #85 뒤로가기·키보드·WCAG AA, #86 모바일. 아트보드는 없다(L-38: 지금 만든 것이 정본).
2. **#9 R2 리사이즈** — 업로드 시 sharp 로 variants 생성 (L-40). 크기 세트와 소급 처리 방식을 거기서 정한다.
3. **에픽 #9 관리자 콘솔** — `npm run admin:create` 로 ADMIN 계정을 먼저 만들고(TOTP 등록까지) #65 가입 심사부터.
   사업자번호 진위 확인 API 는 붙이지 않고 형식 검증만으로 짓는다 (L-22).
4. **#56 로그 적재 · #57 메일 코드** — 발송은 위 "막힌 것" 에 걸려 있지만 코드는 먼저 만들 수 있다.

**아직 정하지 못한 것은 전부 `LATER.md`.** 지금 예약 엔진에 걸린 것은 L-06(슬롯 계산 가정 A1~A12) ·
L-31(합산 잔여) · L-32(매니저가 처리 못 하는 예약)이고, 그중 **A7(심야 영업 중 자정을 넘기면 그 영업일이
빈 결과)** 만 실제 손님이 겪는 문제다.
