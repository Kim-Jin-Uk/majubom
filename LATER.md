# 추후 논의

> 아직 **결정되지 않은 것**만 모은다. "정했는데 아직 안 만든 것"(백로그)은 각 `features/*/README.md` 의 "아직 안 한 것" 에 남는다.
> 항목 번호(L-01…)는 재사용하지 않는다 — 커밋·PR 이 번호로 가리킨다. 결정이 나면 그 줄을 지우고 정본(`majubom-docs/02_기능명세서.md`)이나 해당 README 에 옮겨 적는다.
> 형식: **무엇이 미정인지 / 지금 코드는 어떻게 하고 있는지 / 언제 정해야 하는지**.

## 권한·계정

**L-01 매니저도 매니저를 초대할 수 있게 할까.**
지금은 사장님(OWNER)만 초대한다 — 초대 폼이 OWNER 에게만 보이고 `POST /api/console/members` 는 `OWNER_ONLY` 로 막는다(FR-AUTH-020 그대로).
매니저에게 "구성원 초대" 권한을 하나 더 주는 방식(`permissions.inviteMember`)이면 스키마 변경 없이 된다. 1인 매장이 아니라 점장이 따로 있는 매장을 받을 때 정한다.
→ 관련: `src/features/auth/members.ts`, `src/features/auth/ui/MembersPanel.tsx`, 자원 폼에서 바로 초대하는 흐름(FR-RES-010 "계정 생성 여부")도 같이 정한다.

**L-02 1계정 1사업장을 계속 유지할까.**
지금은 한 계정이 한 사업장에만 속한다(`ALREADY_MEMBER`). 겸업(고객 + 사업자)은 되지만 두 매장의 매니저는 될 수 없다.
프랜차이즈·다점포가 들어오면 `business_members` 를 여러 행으로 두고 세션에 "현재 사업장" 을 실어야 한다 — 프록시·가드·감사 로그가 전부 바뀐다. 그 전에 정할 것.

**L-03 OWNER 이양 · 사업장 삭제.**
둘 다 없다. 사업자가 바뀌거나 폐업하면 지금은 운영자가 DB 로 처리해야 한다. 탈퇴 요구(FR-PRIV)와 함께 정한다.

**L-04 소셜 로그인에서 미검증 이메일이 기존 계정과 부딪힐 때.**
지금은 거부하고 안내만 한다. "기존 계정으로 로그인한 뒤 명시적으로 연결" 화면을 만들지, 이대로 둘지 미정. 카카오 앱 등록(#12) 후 실제 충돌 빈도를 보고 정한다.

**L-05 매니저 비활성화가 겸업 계정의 고객 세션까지 끊는 것.**
지금은 전부 끊는다(초대 계정의 "모든 기기 로그아웃" 보장 쪽). 콘솔 차단 자체는 프록시가 매 요청 `memberStatus` 를 보므로 불필요하다 — 고객으로 쓰던 세션까지 끊는 게 과한지 재검토 여지.

## 예약 엔진

**L-06 `tests/fixtures/README.md` 의 명세 해석 가정 A1~A11.**
슬롯 계산(FR-BOOK-010) 구현이 이 가정대로 40건을 통과했다 — 이제 **명세를 고쳐 가정을 정본으로 올릴지** 가 남았다. 특히:
- **A1 정원 1 = 팀 단위 점유** — 이미 상품 에픽이 이 해석을 코드로 굳혔다(공간형 프리셋 정원 1 + 최대 4명). 명세 문구(`remaining ≥ partySize`)와 어긋나므로 명세를 고치는 쪽으로 정리해야 한다.
- **A7 심야 영업 중 자정이 지나면 그 영업일이 빈 결과** — 20:00~02:00 영업에서 00:10 에 01:00 을 예약할 수 없다. 손님이 가게 안에 있는데 앱으로는 못 잡는 상황이라 실제로 걸릴 것 같다. 고치려면 "영업 중인 전 영업일은 허용" 을 명세에 넣어야 한다.
- **A5 `REQUIRED` 인데 자원을 안 고르면 오류인지, 합산 결과인지** — 구현은 `RESOURCE_REQUIRED` 오류.
- **A4 MODIFIED/EXTRA 결합과 `fixedIgnoreBreaks` 적용 범위.**
- **A9 휴무일 회차를 `OUT_OF_WINDOW` 로 열거할지.**

**L-31 합산 잔여(`Slot.remaining`)가 한 건이 실제로 쓸 수 있는 인원보다 크다.**
자원 무관 조회는 같은 시각을 합산한다(명세·픽스처 S38 이 그렇게 정의). 정원 2 짜리 방이 셋이면 `remaining: 6` 이지만 6명 예약은 어느 방에도 들어가지 않는다.
정원 1 팀 단위(A1)와 정원 N 이 섞이면 "팀 1 + 좌석 8 = 9" 같은 값도 나온다. 예약 생성(FR-BOOK-020)의 인원 검증은 이 합계가 아니라 **자원별 잔여**로 해야 하고,
위젯에 무엇을 보여줄지(합계 vs 한 건 최대)와 함께 정한다.

**L-32 공간·공용 자원 예약은 매니저가 처리할 수 없다.**
FR-BOOK-030 은 "MANAGER 는 본인 담당 건만" 인데, 담당(`resources.member_id`)은 STAFF 자원에만 있다. 그래서 룸·장비 예약의 승인·완료·노쇼는 OWNER 만 할 수 있다.
룸이 여러 개인 매장에서 점장이 처리해야 하면 막힌다. **워크인 대리 등록도 같다** — 담당 자원이 없는 매니저는 아예 쓸 수 없다. 권한 키(`permissions.manageReservations` 같은)로 열지, 자원에 담당 개념을 넣을지 정한다. L-01(매니저 초대 권한)과 함께 보면 좋다.

## 예약 취소·알림

**L-11 휴무일을 등록할 때 겹치는 예약을 어떻게 할까.**
명세는 "일괄 취소 + 고객 알림" 과 "예약 유지" 두 선택지를 준다. 지금은 **유지만** 구현했다(`keepReservations`/`confirmConflicts`) — 조용히 취소하지 않는다.
일괄 취소는 취소 사유·환불·알림 문구가 함께 정해져야 한다. 예약 콘솔 에픽에서 정한다. (`HOLIDAY_BULK_CANCEL` 감사 action 은 이미 있다.)

**L-12 매니저 차단·휴가 신청이 도착했을 때 사장님 알림, 승인·반려 결과의 매니저 알림.**
지금은 근무표 상단 패널이 그 역할을 한다. 알림 에픽에서 채널(푸시/메일)과 즉시성을 정한다.

## 근무표·휴무

**L-13 자정을 넘기는 근무 패턴.**
영업시간은 익일을 지원하는데 근무 패턴은 DB CHECK(`start_time < end_time`)가 막는다 — 규칙이 서로 다르다. 심야 영업 사업장을 받을 때 CHECK 를 완화할지 정한다(`resolve` 는 이미 익일 해석을 지원).

**L-14 "매년 2월 말일" 같은 반복 휴무.**
YEARLY 는 `startDate` 의 월·일로만 판정하고 `month` 컬럼은 저장만 한다. 2/29 는 윤년에만 잡힌다. 지금은 MONTHLY_DAY 말일 + `repeatUntil` 조합으로 우회할 수 있다.

## 상품·매장

**L-15 온보딩 5단계(정책) 완료 판정.**
"기본값에서 바꿨는가" 로 판정한다 — 기본값 그대로가 마음에 드는 사람은 영구히 미완으로 보인다. 선택 단계라 공개에는 영향이 없다. `policy_touched_at` 같은 컬럼을 둘지 정한다.

**L-16 `fixedIgnoreBreaks` 를 상품 설정으로 열지.**
지금은 명세 기본값(true)으로 고정 동작한다. 브레이크를 차감하는 회차형 상품이 실제로 필요해질 때 컬럼을 추가한다. L-06 A4 와 함께 본다.

**L-17 상품 사진의 출처 제한.**
업로드는 R2 지만 폼이 임의의 http(s) URL 도 받는다(업로드 실패 시 우회용). 프로덕션에서 외부 URL 을 계속 허용할지 정한다.

**L-18 타임존 셀렉트.**
값은 `Asia/Seoul` 로 고정 저장한다(IANA 검증은 한다). 해외 사업장이 생기면 UI 를 연다.

**L-19 업종 코드 10종.**
코드가 10종을 고정했는데(`policy-defaults.ts`) 기획서는 "업종별 시작 템플릿을 몇 종으로 할지" 를 열어 뒀다(Q11). 템플릿 수가 정해지면 코드와 맞춘다.

## 스키마·운영

**L-21 `status` 계열을 PG enum 으로 둘지 text + CHECK 로 바꿀지.**
`09_코드관리_검토_리포트` 의 W2 재검토 항목. enum 은 값 추가가 마이그레이션이라 별도 파일이 계속 늘어난다.

**L-22 사업자등록번호 진위 확인 API.**
지금은 형식(체크섬)만 본다. 등록증 이미지 첨부 컬럼도 없다. 관리자 심사(FR-ADM-010)를 실제로 돌릴 때 국세청 API 연동 여부와 함께 정한다. (기획서 Q2)

**L-23 법정 보존기간과 삭제 정책의 충돌.**
채팅 1년 삭제·예약 1년 마스킹이 전자상거래법(5년/3년)과 부딪힐 수 있다. 잠정 방향은 "삭제 대신 열람 제한". **법무 확인이 필요한 항목.** (기획서 Q10)

## 제품 범위 (기획서·검토 리포트에 결정 미기록)

정본은 `majubom-docs` 다 — 여기서는 잊지 않으려고 이름만 남긴다.

**L-24** 채팅(M8)을 MVP 에 넣을지 (유지 / 출시 후 / 제외 + 카카오채널 링크). 리뷰어 권고는 제외. `05` D1 — 코드는 `FEATURE_CHAT` 플래그로 꺼 둘 수 있게만 해 뒀다.
**L-25** 홈페이지 빌더(M6)의 깊이 — 12열 그리드 vs "순서 + 콘텐츠 + 테마". `05` D2.
**L-26** 전체 일정 16주 vs 20~22주 — L-24·L-25 에 딸린다. `05` D3.
**L-27** 비회원 예약 재도입 — 로그인 전환율이 70% 아래면 재논의. `02` §비회원.
**L-28** 채팅 미응답 시 사업장 노출 강등. 기획서 Q8(출시 후).
**L-29** 상표·도메인 확인('마주,봄'). 기획서 Q9.
**L-30** 공개 저장소 라이선스 — 지금은 all rights reserved. 기획서 Q12.
