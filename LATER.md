# 추후 논의

> 아직 **결정되지 않은 것**만 모은다. "정했는데 아직 안 만든 것"(백로그)은 각 `features/*/README.md` 의 "아직 안 한 것" 에 남는다.
> 항목 번호(L-01…)는 재사용하지 않는다 — 커밋·PR 이 번호로 가리킨다. 결정이 나면 그 줄을 지우고 정본(`majubom-docs/02_기능명세서.md`)이나 해당 README 에 옮겨 적는다.
> 형식: **무엇이 미정인지 / 지금 코드는 어떻게 하고 있는지 / 언제 정해야 하는지**.

## 권한·계정

**L-01 매니저도 매니저를 초대할 수 있게 할까.**
지금은 사장님(OWNER)만 초대한다 — 초대 폼이 OWNER 에게만 보이고 `POST /api/console/members` 는 `OWNER_ONLY` 로 막는다(FR-AUTH-020 그대로).
매니저에게 "구성원 초대" 권한을 하나 더 주는 방식(`permissions.inviteMember`)이면 스키마 변경 없이 된다. 1인 매장이 아니라 점장이 따로 있는 매장을 받을 때 정한다.
→ 관련: `src/features/auth/members.ts`, `src/features/auth/ui/MembersPanel.tsx`, 자원 폼에서 바로 초대하는 흐름(FR-RES-010 "계정 생성 여부")도 같이 정한다.

**L-02 1계정 1사업장을 계속 유지할까.**
지금은 한 계정이 한 사업장에만 속한다(`ALREADY_MEMBER`). 겸업(고객 + 사업자)은 되지만 두 매장의 매니저는 될 수 없다.
프랜차이즈·다점포가 들어오면 `business_members` 를 여러 행으로 두고 세션에 "현재 사업장" 을 실어야 한다 — 프록시·가드·감사 로그가 전부 바뀐다. 그 전에 정할 것.

**L-03 OWNER 이양 · 사업장 삭제.**
둘 다 없다. 사업자가 바뀌거나 폐업하면 지금은 운영자가 DB 로 처리해야 한다. 탈퇴 요구(FR-PRIV)와 함께 정한다.

**L-04 소셜 로그인에서 미검증 이메일이 기존 계정과 부딪힐 때.**
지금은 거부하고 안내만 한다. "기존 계정으로 로그인한 뒤 명시적으로 연결" 화면을 만들지, 이대로 둘지 미정. 카카오 앱 등록(#12) 후 실제 충돌 빈도를 보고 정한다.

**L-05 매니저 비활성화가 겸업 계정의 고객 세션까지 끊는 것.**
지금은 전부 끊는다(초대 계정의 "모든 기기 로그아웃" 보장 쪽). 콘솔 차단 자체는 프록시가 매 요청 `memberStatus` 를 보므로 불필요하다 — 고객으로 쓰던 세션까지 끊는 게 과한지 재검토 여지.

## 예약 엔진

**L-06 `tests/fixtures/README.md` 의 명세 해석 가정 A1~A12.** — **해소 (9/10): 명세를 고치는 쪽으로 정리했다.**
`majubom-docs/02_기능명세서.md` FR-BOOK-010 에 반영했다 — 날짜 범위(A7) · 잔여 정원 분기와 "정원의 두 가지 의미"(A1) ·
FR-SITE-020 인원 상한 단서 · 프리셋 표 각주 · "구현 해석 고정" 표(나머지 A2·A3·A4·A5·A6·A8·A9·A10·A11·A12).
**docs 저장소 커밋은 사업자(사람) 몫이다** — 이 저장소에서 밀 수 없다. 아래는 그때 무엇을 왜 고쳤는지의 기록이다.
- **A1 정원 1 = 팀 단위 점유** — 이미 상품 에픽이 이 해석을 코드로 굳혔다(공간형 프리셋 정원 1 + 최대 4명). 명세 문구(`remaining ≥ partySize`)를 문자 그대로 두면 그 프리셋에서 2명 이상이 영원히 예약 불가라, 명세를 고쳤다.
- ~~**A7 심야 영업 중 자정이 지나면 그 영업일이 빈 결과**~~ → **해소 (9/10): 지금 영업 중인 영업일은 어제여도 연다.** `slots.ts` 의 `dateInRange` 로 옮겼고 예약 생성도 같은 함수를 쓴다(따로 적어 두면 "보이는데 예약은 안 되는 시각" 이 생긴다). 픽스처 S41 이 그 동작을, S28 이 "열렸지만 선행시간에 걸린다" 를 본다. **명세 §3.7 0단계에 문장을 넣어야 한다** — 아래 "명세에 올릴 것" 참고.
- A4·A5·A9 를 비롯한 나머지는 "구현 해석 고정" 표로 한 번에 올렸다.

**L-31 합산 잔여(`Slot.remaining`)가 한 건이 실제로 쓸 수 있는 인원보다 크다.**
자원 무관 조회는 같은 시각을 합산한다(명세·픽스처 S38 이 그렇게 정의). 정원 2 짜리 방이 셋이면 `remaining: 6` 이지만 6명 예약은 어느 방에도 들어가지 않는다.
정원 1 팀 단위(A1)와 정원 N 이 섞이면 "팀 1 + 좌석 8 = 9" 같은 값도 나온다. 예약 생성(FR-BOOK-020)의 인원 검증은 이 합계가 아니라 **자원별 잔여**로 해야 하고,
위젯에 무엇을 보여줄지(합계 vs 한 건 최대)와 함께 정한다.

**L-32 매니저가 처리할 수 없는 예약이 있다 — 공간·공용 자원 건 전부와, 담당 변경.**
FR-BOOK-030 은 "MANAGER 는 본인 담당 건만" 인데, 담당(`resources.member_id`)은 STAFF 자원에만 있다. 그래서 룸·장비 예약의 승인·완료·노쇼는 OWNER 만 할 수 있다.
룸이 여러 개인 매장에서 점장이 처리해야 하면 막힌다. **워크인 대리 등록도 같다** — 담당 자원이 없는 매니저는 아예 쓸 수 없다.
**담당·공간 변경(FR-BOOK-080)은 한 발 더 나가 자원 종류와 무관하게 OWNER 전용이다** — 옮기는 것은 남의 일정을 건드리는 일이라 본인 담당이어도 열지 않았다.
`permissions.viewAllReservations` 는 **보이는 범위**만 넓힌다(처리 권한이 아니다) — 처리까지 열려면 권한 키(`permissions.manageReservations` 같은)를 따로 두든지, 자원에 담당 개념을 넣어야 한다. L-01(매니저 초대 권한)과 함께 보면 좋다.

**L-35 콘솔 지표가 FIXED 상품의 브레이크 회차를 어떻게 볼지.**
`slots.ts` 는 FIXED 상품에서 영업시간 브레이크를 차감하지 않는다(`fixedIgnoreBreaks` 기본 true — 요가원의 점심 브레이크는 수업 금지 시간이 아니다).
그런데 콘솔 지표는 사업장 단위라 상품별 설정을 알 수 없다. 지금은 **분자(판 시간)는 브레이크를 포함한 구간으로 재고, 분모(운영 가능 시간)는 브레이크를 뺀다** —
브레이크에 든 회차가 통째로 사라지는 것보다 낫다고 봤다. 대신 그런 상품이 있으면 가동률이 100% 에 붙는다.
FIXED 상품을 실제로 파는 사업장을 받으면 사업장 단위 "브레이크에도 판다" 플래그를 둘지 정한다.

**L-33 가동률에서 운영시간 밖으로 나간 버퍼를 셀지.**
명세는 "확정·완료 예약의 점유 시간 합 ÷ 운영 가능 시간 합" 이라고만 한다. 점유는 버퍼를 포함하므로 개점 전·폐점 후로 나갈 수 있고, 그대로 더하면 100% 를 넘는다.
지금은 **운영시간으로 잘라서** 센다(`dashboard.ts` `utilization`) — "가진 시간 중 얼마나 찼나" 를 묻는 숫자라고 읽었다. 청소 시간까지 "일한 시간" 으로 보고 싶다는 요구가 나오면 그때 정한다.

**L-34 주 캘린더의 자원별 컬럼.**
명세는 일·주 모두 자원별 컬럼이라고 하는데, 자원이 N 개인 주 뷰는 7 × N 컬럼이라 데스크톱에서도 못 읽는다.
지금은 **일 = 자원 컬럼 / 주 = 날짜 컬럼**이고 주 뷰 블록에 자원명을 붙인다.
자원이 2~3개일 때만 주에도 자원 컬럼을 줄지, 자원 선택을 강제할지 — **담당자가 3명 넘는 매장을 받아 "주간에 누가 언제인지 안 보인다" 는 말이 나올 때** 정한다.

## 예약 취소·알림

**L-11 휴무일을 등록할 때 겹치는 예약을 어떻게 할까.**
명세는 "일괄 취소 + 고객 알림" 과 "예약 유지" 두 선택지를 준다. 지금은 **유지만** 구현했다(`keepReservations`/`confirmConflicts`) — 조용히 취소하지 않는다.
일괄 취소는 취소 사유·환불·알림 문구가 함께 정해져야 한다 — 문구가 알림 에픽에 걸려 있어 예약 콘솔 에픽(#8)에서도 정하지 못했다. **알림 에픽에서 정한다.** (`HOLIDAY_BULK_CANCEL` 감사 action 은 이미 있다.)

**L-12 매니저 차단·휴가 신청이 도착했을 때 사장님 알림, 승인·반려 결과의 매니저 알림.**
지금은 근무표 상단 패널이 그 역할을 한다. 알림 에픽에서 채널(푸시/메일)과 즉시성을 정한다.

## 근무표·휴무

**L-13 자정을 넘기는 근무 패턴.**
영업시간은 익일을 지원하는데 근무 패턴은 DB CHECK(`start_time < end_time`)가 막는다 — 규칙이 서로 다르다. 심야 영업 사업장을 받을 때 CHECK 를 완화할지 정한다(`resolve` 는 이미 익일 해석을 지원).

**L-14 "매년 2월 말일" 같은 반복 휴무.**
YEARLY 는 `startDate` 의 월·일로만 판정하고 `month` 컬럼은 저장만 한다. 2/29 는 윤년에만 잡힌다. 지금은 MONTHLY_DAY 말일 + `repeatUntil` 조합으로 우회할 수 있다.

## 상품·매장

**L-15 온보딩 5단계(정책) 완료 판정.**
"기본값에서 바꿨는가" 로 판정한다 — 기본값 그대로가 마음에 드는 사람은 영구히 미완으로 보인다. 선택 단계라 공개에는 영향이 없다. `policy_touched_at` 같은 컬럼을 둘지 정한다.

**L-16 `fixedIgnoreBreaks` 를 상품 설정으로 열지.**
지금은 명세 기본값(true)으로 고정 동작한다. 브레이크를 차감하는 회차형 상품이 실제로 필요해질 때 컬럼을 추가한다. L-06 A4 와 함께 본다.

**L-17 상품 사진의 출처 제한.**
업로드는 R2 지만 폼이 임의의 http(s) URL 도 받는다(업로드 실패 시 우회용). 프로덕션에서 외부 URL 을 계속 허용할지 정한다.

**L-18 타임존 셀렉트.**
값은 `Asia/Seoul` 로 고정 저장한다(IANA 검증은 한다). 해외 사업장이 생기면 UI 를 연다.

**L-19 업종 코드 10종.**
코드가 10종을 고정했는데(`policy-defaults.ts`) 기획서는 "업종별 시작 템플릿을 몇 종으로 할지" 를 열어 뒀다(Q11).
**열어 둔 채로 간다 (9/10 결정)** — 베타에서 실제 업종 분포를 보고 정한다. 그때까지 `categoryLabel` 이 모르는 코드를 "기타" 로 떨어뜨리는 것이 이 결정의 안전망이다.

**L-36 공개 홈의 휴무 안내를 몇 개까지 보여줄까.**
앞으로 60일 안의 사업장 전체 휴무를 전부 내보낸다 — 매주 휴무 규칙이 둘이면 열여덟 줄이 된다.
지금은 자르지 않는다(자르면 "이 뒤에도 있음" 을 알릴 방법이 없어 더 나쁘다). 실제로 규칙을 여러 개 쓰는 사업장을 받으면
"이번 달" 로 좁힐지, 접기를 둘지 정한다.

**L-40 R2 이미지 리사이즈는 업로드 시 sharp 로 만든다 (9/10 결정).**
Cloudflare Images(유료)를 쓰지 않는다. 업로드 시 서버가 variants 를 만들어 R2 에 같이 올리고, 원본과 함께 보관한다.
정해야 남은 것은 variants 크기 세트와 "이미 올라간 사진은 어떻게 할지"(소급 생성 배치 vs 요청 시 생성)다. #9 에서 정한다.

**L-41 1기 게이트는 noindex 만 유지한다 (9/10 결정).**
`GATE_BASIC_AUTH` 를 걸지 않는다 — 주소를 아는 사람은 들어올 수 있고 색인만 막힌 상태다. 베타(W17) 직전에 다시 본다.
프록시의 캐시 규칙이 Basic Auth 를 켜는 순간 `private, no-store` 로 바뀌는 것(`features/site/README.md`)만 기억해 둔다.

**L-38 공개 홈 모바일 레이아웃을 아트보드와 대조하지 못했다.**
`10_이슈_계층.json` 은 #77 의 근거로 "디자인 MobileHome 아트보드" 를 가리키는데, 그 파일(`reservation-design`)이
저장소에 없다. 지금 폰 화면은 아트보드가 아니라 원칙으로 만들었다 — 상호·밝기 토글 sticky 줄, 폰 전용 전화·길찾기 바,
420px 이하에서 썸네일·여백 축소. **아트보드가 따로 없다고 확인됐다 (9/10) — 지금 만든 것이 사실상의 정본이다.** #86 모바일 예약 플로우도 같은 방식으로 만들었다.

**L-39 손님이 누른 밝기를 사업장마다 따로 기억할지.**
지금은 `site-theme` 한 열쇠라, 한 가게에서 다크를 누르면 다른 가게 홈도 다크로 열린다.
밝기는 눈의 문제라 이 편이 맞다고 봤는데, 브랜드 색이 붙는 빌더(에픽 #15) 뒤에도 같은 판단인지는 그때 다시 본다.
되돌리는 버튼(= 사업자 선택으로 복귀)도 그때 같이 정한다 — 지금 토글은 라이트/다크 두 상태뿐이다.

**L-37 공개 홈의 사진을 어디서 가져올까.**
커버와 갤러리가 **상품 사진**이다 — 가게 사진이라는 개념이 아직 없어서다. 상품이 하나면 커버와 상품 썸네일이 같은 사진이 된다.
브랜드 온보딩 단계(4단계)에서 커버·로고를 따로 받을지, 지금처럼 상품 사진을 재활용할지 그때 정한다.

## 스키마·운영

**L-21 `status` 계열을 PG enum 으로 둘지 text + CHECK 로 바꿀지.**
`09_코드관리_검토_리포트` 의 W2 재검토 항목. enum 은 값 추가가 마이그레이션이라 별도 파일이 계속 늘어난다.

**L-22 사업자등록번호 진위 확인 API.** — **붙인다. 시점은 뒤로 미룸 (9/10 결정)**
지금은 형식(체크섬)만 본다. 등록증 이미지 첨부 컬럼도 없다. 관리자 심사(#65)는 **형식 검증만으로 먼저 만든다** —
API 자리는 비워 두되 심사 화면이 그것을 전제하지 않게 짓는다. 국세청 API 키·요금이 정해지면 판정 한 줄을 갈아 끼운다. (기획서 Q2)

**L-23 법정 보존기간과 삭제 정책의 충돌.** — **필요하나 시점은 뒤로 미룸 (9/10 결정)**
채팅 1년 삭제·예약 1년 마스킹이 전자상거래법(5년/3년)과 부딪힐 수 있다. 잠정 방향은 "삭제 대신 열람 제한".
**법무 확인이 필요한 항목**이고, 약관을 쓰는 시점(W12)까지는 답이 있어야 한다 — C5 마스킹 배치(#130)를 그 전에 켜지 않는다. (기획서 Q10)

## 제품 범위 (기획서·검토 리포트에 결정 미기록)

정본은 `majubom-docs` 다 — 여기서는 잊지 않으려고 이름만 남긴다.

**L-24** 채팅(M8)을 MVP 에 넣을지 (유지 / 출시 후 / 제외 + 카카오채널 링크). 리뷰어 권고는 제외. `05` D1 — 코드는 `FEATURE_CHAT` 플래그로 꺼 둘 수 있게만 해 뒀다.
**L-25** 홈페이지 빌더(M6)의 깊이 — 12열 그리드 vs "순서 + 콘텐츠 + 테마". `05` D2.
**L-26** 전체 일정 16주 vs 20~22주 — L-24·L-25 에 딸린다. `05` D3.
**L-27** 비회원 예약 재도입 — 로그인 전환율이 70% 아래면 재논의. `02` §비회원.
**L-28** 채팅 미응답 시 사업장 노출 강등. 기획서 Q8(출시 후).
**L-29** 상표·도메인 확인('마주,봄'). 기획서 Q9.
**L-30** 공개 저장소 라이선스 — 지금은 all rights reserved. 기획서 Q12.
